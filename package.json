{
  "name": "sonar-test-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./build && mkdir build",
    "build": "npm run clean && babel ./src -d ./build",
    "start": "npm run build && node ./build/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.22.10",
    "@babel/core": "^7.22.10",
    "@babel/node": "^7.22.10",
    "@babel/preset-env": "^7.22.10",
    "rimraf": "^5.0.1"
  }
}
